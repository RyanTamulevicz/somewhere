{
  "name": "address-input",
  "version": "1.0.0",
  "description": "International address input web component using Google's address metadata",
  "type": "module",
  "main": "dist/address-input.js",
  "types": "dist/address-input.d.ts",
  "scripts": {
    "build": "tsc && npm run build:css && npm run build:demo && npm run build:cdn",
    "build:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify",
    "build:demo": "vite build",
    "build:cdn": "vite build --config vite.cdn.config.js",
    "dev": "concurrently \"tsc --watch\" \"npm run dev:css\" \"vite\"",
    "dev:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "fetch-data": "node scripts/fetch-address-data.js",
    "prepare": "npm run fetch-data && npm run build",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:cdn": "wrangler deploy --config wrangler.cdn.jsonc"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^9.2.1",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.3.0",
    "vite": "^7.3.1",
    "wrangler": "^3.107.3"
  },
  "keywords": [
    "web-components",
    "address",
    "international",
    "lit",
    "input"
  ],
  "license": "MIT"
}